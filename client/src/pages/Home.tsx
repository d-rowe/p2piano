import { Box } from '@chakra-ui/react'
import RoomCard from '../components/RoomCard';

const Home = () => (
    <Box
        bg='black'
        h='full'
    >
        <RoomCard />
    </Box>
);

export default Home;
